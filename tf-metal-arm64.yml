name: pong
channels:
  - apple
  - conda-forge
dependencies:
  - python=3.9.1  ## specify desired version
  - pip
  - tensorflow-deps
  - numpy
  - notebook
  - nb_conda_kernels

  ## PyPI packages
  - pip:
    - tensorflow-macos
    - tensorflow-metal  ## optional, but recommended
    - pygame
    - tqdm